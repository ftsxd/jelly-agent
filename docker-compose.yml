services:
  jelly-agent:
    build: .
    ports:
      - "6185:6185"
    environment:
      JELLY_CONFIG: /data/config.yaml
    volumes:
      - jelly-data:/data
    restart: unless-stopped

volumes:
  jelly-data:
